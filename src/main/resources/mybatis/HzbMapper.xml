<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xnjr.mall.dao.IHzbDAO">
	<resultMap id="hzb_DB_Result" type="hzb">
		<result column="code" property="code" />
		<result column="name" property="name" />
		<result column="pic" property="pic" />
		<result column="description" property="description" />
		<result column="price" property="price" />
		<result column="currency" property="currency" />
		<result column="system_code" property="systemCode" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="code != null and code != '' ">
				AND t.code = #{code}
			</if>
		</trim>
	</sql>
	<select id="select_hzb" parameterType="hzb" resultMap="hzb_DB_Result">
		SELECT
		t.code
		,t.name
		,t.pic
		,t.description
		,t.price
		,t.currency
		,t.system_code
		FROM
		tact_hzb t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_hzb_count" parameterType="hzb"
		resultType="java.lang.Long">
		SELECT count(1) FROM tact_hzb t
		<include refid="where_condition" />
	</select>

	<insert id="insert_hzb" parameterType="hzb">
		INSERT INTO
		tact_hzb(
		code
		,name
		,pic
		,description
		,price
		,currency
		,system_code
		)
		VALUES(
		#{code}
		,#{name}
		,#{pic}
		,#{description}
		,#{price}
		,#{currency}
		,#{systemCode}
		)
	</insert>

	<update id="update_hzb" parameterType="hzb">
		UPDATE tact_hzb
		SET
		name = #{name}
		,pic = #{pic}
		,description = #{description}
		,price = #{price}
		,currency = #{currency}
		,system_code = #{systemCode}
		WHERE code = #{code}
	</update>


</mapper>