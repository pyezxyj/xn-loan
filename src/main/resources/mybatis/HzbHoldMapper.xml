<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xnjr.mall.dao.IHzbHoldDAO">
	<resultMap id="hzbHold_DB_Result" type="hzbHold">
		<result column="id" property="id" />
		<result column="user_id" property="userId" />
		<result column="hzb_code" property="hzbCode" />
		<result column="status" property="status" />
		<result column="price" property="price" />
		<result column="currency" property="currency" />
		<result column="period_rock_num" property="periodRockNum" />
		<result column="total_rock_num" property="totalRockNum" />
		<result column="system_code" property="systemCode" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
		<if test="id != null and id != '' ">
				AND t.id = #{id}
			</if>
			<if test="status != null and status != '' ">
				AND t.status = #{status}
			</if>
			<if test="userId != null and userId != '' ">
				AND t.user_id = #{userId}
			</if>
			<if test="systemCode != null and systemCode != '' ">
				AND t.system_code = #{systemCode}
			</if>
		</trim>
	</sql>
	<select id="select_hzbHold" parameterType="hzbHold" resultMap="hzbHold_DB_Result">
		SELECT
		t.id
		,t.user_id
		,t.hzb_code
		,t.status
		,t.price
		,t.currency
		,t.period_rock_num
		,t.total_rock_num
		,t.system_code
		FROM
		tact_hzb_hold t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_hzbHold_count" parameterType="hzbHold"
		resultType="java.lang.Long">
		SELECT count(1) FROM tact_hzb_hold t
		<include refid="where_condition" />
	</select>

	<insert id="insert_hzbHold" parameterType="hzbHold">
		INSERT INTO
		tact_hzb_hold(
		user_id
		,hzb_code
		,status
		,price
		,currency
		,period_rock_num
		,total_rock_num
		,system_code
		)
		VALUES(
		#{userId}
		,#{hzbCode}
		,#{status}
		,#{price}
		,#{currency}
		,#{periodRockNum}
		,#{totalRockNum}
		,#{systemCode}
		)
	</insert>

	<update id="update_hzbHold" parameterType="hzbHold">
		UPDATE tact_hzb_hold
		SET
		user_id = #{userId}
		,hzb_code = #{hzbCode}
		,status = #{status}
		,price = #{price}
		,currency = #{currency}
		,period_rock_num = #{periodRockNum}
		,total_rock_num = #{totalRockNum}
		,system_code = #{systemCode}
		WHERE code = #{code}
	</update>
	
	<update id="update_status" parameterType="hzbHold">
		UPDATE tact_hzb_hold
		SET
		status = #{status}
		WHERE id = #{id}
	</update>


</mapper>